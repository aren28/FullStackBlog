export type PostType = {
  id: number;
  title: string;
  description: string;
  DateTime: string;
};

export type PostBlogType = {
  title: string;
  description: string;
  userProfileId: string;
};
